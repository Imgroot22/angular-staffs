import { BasicStructDirDirective } from './basic-struct-dir.directive';

describe('BasicStructDirDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicStructDirDirective();
    expect(directive).toBeTruthy();
  });
});
