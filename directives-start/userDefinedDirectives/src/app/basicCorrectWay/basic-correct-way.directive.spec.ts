import { BasicCorrectWayDirective } from './basic-correct-way.directive';

describe('BasicCorrectWayDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicCorrectWayDirective();
    expect(directive).toBeTruthy();
  });
});
